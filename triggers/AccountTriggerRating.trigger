trigger AccountTriggerRating on Account (before update) {

    /*For(Account act : Trigger.New){
        If(Trigger.oldMap.get(act.Id).Rating == 'Hot' && act.Rating != 'Hot'){
            act.addError('Não pode tirar a conta cabeção!!!!!!');
        }
    }*/
}